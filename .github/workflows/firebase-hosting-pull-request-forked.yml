# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
# For PRs from forked repositories. We use the 'pull_request_target'
#   event, to be able to secrets for deployments of these prs. And it protects 
#    us from potentially unsafe code.

name: Deploy to Firebase Hosting on PR
on:
  pull_request_target:
jobs:
  call-workflow-deploy-clm:
    if: '${{ github.event.pull_request.head.repo.full_name != github.repository }}'
    uses: ./.github/workflows/reusable-deploy-project-workflow.yml
    with:
      environment: elewa-dev
      project_id: ele-convl-manager-7cd0a
      channelId: null
    secrets: inherit
