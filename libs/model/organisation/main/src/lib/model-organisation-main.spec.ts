import { modelOrganisationMain } from './model-organisation-main';

describe('modelOrganisationMain', () => {
  it('should work', () => {
    expect(modelOrganisationMain()).toEqual('model-organisation-main');
  });
});
