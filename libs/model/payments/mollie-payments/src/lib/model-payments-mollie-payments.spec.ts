import { modelPaymentsMolliePayments } from './model-payments-mollie-payments';

describe('modelPaymentsMolliePayments', () => {
  it('should work', () => {
    expect(modelPaymentsMolliePayments()).toEqual(
      'model-payments-mollie-payments'
    );
  });
});
