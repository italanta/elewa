import { modelConvsMgrMicroAppBase } from './model-convs-mgr-micro-app-base';

describe('modelConvsMgrMicroAppBase', () => {
  it('should work', () => {
    expect(modelConvsMgrMicroAppBase()).toEqual(
      'model-convs-mgr-micro-app-base'
    );
  });
});
