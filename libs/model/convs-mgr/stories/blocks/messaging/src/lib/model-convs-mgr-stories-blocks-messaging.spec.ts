import { modelConvsMgrStoriesBlocksMessaging } from './model-convs-mgr-stories-blocks-messaging';

describe('modelConvsMgrStoriesBlocksMessaging', () => {
  it('should work', () => {
    expect(modelConvsMgrStoriesBlocksMessaging()).toEqual(
      'model-convs-mgr-stories-blocks-messaging'
    );
  });
});
