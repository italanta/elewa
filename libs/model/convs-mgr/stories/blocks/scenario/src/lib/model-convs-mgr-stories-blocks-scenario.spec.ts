import { modelConvsMgrStoriesBlocksScenario } from './model-convs-mgr-stories-blocks-scenario';

describe('modelConvsMgrStoriesBlocksScenario', () => {
  it('should work', () => {
    expect(modelConvsMgrStoriesBlocksScenario()).toEqual(
      'model-convs-mgr-stories-blocks-scenario'
    );
  });
});
