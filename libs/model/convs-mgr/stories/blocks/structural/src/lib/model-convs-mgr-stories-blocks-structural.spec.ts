import { modelConvsMgrStoriesBlocksStructural } from './model-convs-mgr-stories-blocks-structural';

describe('modelConvsMgrStoriesBlocksStructural', () => {
  it('should work', () => {
    expect(modelConvsMgrStoriesBlocksStructural()).toEqual(
      'model-convs-mgr-stories-blocks-structural'
    );
  });
});
