import { modelConvsMgrStoriesBlocksMain } from './model-convs-mgr-stories-blocks-main';

describe('modelConvsMgrStoriesBlocksMain', () => {
  it('should work', () => {
    expect(modelConvsMgrStoriesBlocksMain()).toEqual(
      'model-convs-mgr-stories-blocks-main'
    );
  });
});
