import { modelConvsMgrStoriesFlows } from './model-convs-mgr-stories-flows';

describe('modelConvsMgrStoriesFlows', () => {
  it('should work', () => {
    expect(modelConvsMgrStoriesFlows()).toEqual(
      'model-convs-mgr-stories-flows'
    );
  });
});
