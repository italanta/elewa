import { modelConvsMgrStoriesMain } from './model-convs-mgr-stories-main';

describe('modelConvsMgrStoriesMain', () => {
  it('should work', () => {
    expect(modelConvsMgrStoriesMain()).toEqual('model-convs-mgr-stories-main');
  });
});
