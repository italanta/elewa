import { modelConvsMgrFunctions } from './model-convs-mgr-functions';

describe('modelConvsMgrFunctions', () => {
  it('should work', () => {
    expect(modelConvsMgrFunctions()).toEqual('model-convs-mgr-functions');
  });
});
