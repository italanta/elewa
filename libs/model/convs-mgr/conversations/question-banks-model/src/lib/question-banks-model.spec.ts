import { questionBanksModel } from './question-banks-model';

describe('questionBanksModel', () => {
  it('should work', () => {
    expect(questionBanksModel()).toEqual('question-banks-model');
  });
});
