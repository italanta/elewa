import { elementsBaseSecurityConfig } from './elements-base-security-config';

describe('elementsBaseSecurityConfig', () => {
  it('should work', () => {
    expect(elementsBaseSecurityConfig()).toEqual(
      'elements-base-security-config'
    );
  });
});
