# private-functions-organisation

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test private-functions-organisation` to execute the unit tests via [Jest](https://jestjs.io).

## Running lint

Run `nx lint private-functions-organisation` to execute the lint via [ESLint](https://eslint.org/).
