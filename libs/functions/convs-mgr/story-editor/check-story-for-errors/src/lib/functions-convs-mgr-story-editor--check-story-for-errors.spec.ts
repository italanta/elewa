import { functionsConvsMgrStoryEditorCheckStoryForErrors } from './functions-convs-mgr-story-editor--check-story-for-errors';

describe('functionsConvsMgrStoryEditorCheckStoryForErrors', () => {
  it('should work', () => {
    expect(functionsConvsMgrStoryEditorCheckStoryForErrors()).toEqual(
      'functions-convs-mgr-story-editor--check-story-for-errors'
    );
  });
});
