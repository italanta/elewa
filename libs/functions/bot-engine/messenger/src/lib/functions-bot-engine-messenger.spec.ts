import { functionsBotEngineMessenger } from './functions-bot-engine-messenger';

describe('functionsBotEngineMessenger', () => {
  it('should work', () => {
    expect(functionsBotEngineMessenger()).toEqual(
      'functions-bot-engine-messenger'
    );
  });
});
