import { WebhookBlockService } from "../block-type/webhook-block.service";

describe("WebhookBlockService", () =>
{
  let incomingHttpRequest: WebhookBlockService;

  beforeEach(() =>
  {
    incomingHttpRequest = new WebhookBlockService();

  });
  describe("incomingHttpRequest", () =>
  {
    // Create a sample incoming whatsapp sticker message payload
    const dataCollection 
    };

});