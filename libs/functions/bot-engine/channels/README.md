# functions-bot-engine-channels

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test functions-bot-engine-channels` to execute the unit tests via [Jest](https://jestjs.io).
