import { functionsBotEngineChannels } from './functions-bot-engine-channels';

describe('functionsBotEngineChannels', () => {
  it('should work', () => {
    expect(functionsBotEngineChannels()).toEqual(
      'functions-bot-engine-channels'
    );
  });
});
