import { functionsBotEngineWhatsapp } from './functions-bot-engine-whatsapp';

describe('functionsBotEngineWhatsapp', () => {
  it('should work', () => {
    expect(functionsBotEngineWhatsapp()).toEqual(
      'functions-bot-engine-whatsapp'
    );
  });
});
