import { interactiveVoiceResponse } from './interactive-voice-response';

describe('interactiveVoiceResponse', () => {
  it('should work', () => {
    expect(interactiveVoiceResponse()).toEqual('interactive-voice-response');
  });
});
