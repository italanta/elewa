import { stateConvsMgrStoriesMain } from './state-convs-mgr-stories-main';

describe('stateConvsMgrStoriesMain', () => {
  it('should work', () => {
    expect(stateConvsMgrStoriesMain()).toEqual('state-convs-mgr-stories-main');
  });
});
