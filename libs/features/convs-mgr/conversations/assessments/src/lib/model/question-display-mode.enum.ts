/**
 * Enum representation for when a question is either in view mode or edit mode
 * Edit mode means a content creator is editing a question
 */
export enum QuestionDisplayMode 
{
  EDITING = 1,
  VIEWING = 2,
}
