import { assessmentMicroApp } from './features-convs-mgr-stories-blocks-library-assessment-micro-app';

describe('featuresConvsMgrStoriesBlocksLibraryAssessmentMicroApp', () => {
  it('should work', () => {
    expect(assessmentMicroApp()).toEqual(
      'features-convs-mgr-stories-blocks-library-assessment-micro-app'
    );
  });
});
